#!/usr/bin/env bash
sudo apt purge -y htop
sudo apt purge -y tmux
sudo apt purge -y zsh
sudo apt purge -y neovim
sudo apt purge -y curl
sudo apt purge -y lsof
sudo apt purge -y i3
sudo apt purge -y docker
sudo apt purge -y docker-compose
